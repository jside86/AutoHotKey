;  Make selected text UPPERCASE - Alt + Shift + U
+!u::
{
    A_Clipboard := ""               ; Clear clipboard
    Send("^c")                      ; Copy selection
    if ClipWait(2)                  ; Wait up to 2s for text
    {
        A_Clipboard := StrUpper(A_Clipboard)
        Send("^v")                  ; Paste uppercase text
    }
}
